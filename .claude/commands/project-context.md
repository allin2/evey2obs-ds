阅读 `CLAUDE.md`、`README.md`、`docs/PRODUCT_REQUIREMENTS.html`、
`docs/ARCHITECTURE.md`、`docs/IMPLEMENTATION_PLAN.md`、`docs/PROJECT_STATUS.md` 和 `pyproject.toml`。

然后检查当前代码与测试，用中文输出：

1. 项目目标与边界。
2. 当前已实现能力，不得将 PRD 中的计划误报为已实现。
3. 当前实施阶段与最大风险。
4. 下一个范围最小、价值最高的实现切片。
5. 该切片的文件范围与验证命令。

不读取或对照任何旧项目代码。本命令只用于建立上下文，不修改文件。
