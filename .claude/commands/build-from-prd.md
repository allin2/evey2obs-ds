将用户的请求视为“按照 PRD 从零构建 evey2obs-ds”。

读取 `CLAUDE.md`、`docs/PRODUCT_REQUIREMENTS.html`、`docs/ARCHITECTURE.md`、
`docs/IMPLEMENTATION_PLAN.md` 和 `docs/PROJECT_STATUS.md`。

然后按 `/implement-next` 的工作流执行下一个未完成切片。
在当前切片完整实现、测试、审查和更新状态前不停止。
不读取、复制、导入或对照任何旧项目代码。
